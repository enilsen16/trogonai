# ── NATS ──────────────────────────────────────────────────────────────────────
NATS_URL=nats://nats:4222
# Optional auth (pick one):
# NATS_TOKEN=my-nats-token
# NATS_USER=user
# NATS_PASSWORD=password
# NATS_NKEY=SUANKEYSEED...
# NATS_CREDS=/run/secrets/nats.creds

# ── Proxy ─────────────────────────────────────────────────────────────────────
PROXY_PREFIX=trogon
PROXY_PORT=8080
PROXY_WORKER_TIMEOUT_SECS=60

# ── Worker ────────────────────────────────────────────────────────────────────
WORKER_CONSUMER_NAME=proxy-workers

# ── Vault token seeding ───────────────────────────────────────────────────────
# Format: VAULT_TOKEN_<token>=<real_api_key>
# The worker reads these at startup and seeds the in-memory vault.
#
# NEVER commit this file with real keys — use Docker secrets or a secret manager
# in production (replace MemoryVault with a HashiCorp Vault / AWS SM backend).
VAULT_TOKEN_tok_anthropic_prod_demo=sk-ant-REPLACE_ME
VAULT_TOKEN_tok_openai_prod_demo=sk-REPLACE_ME
VAULT_TOKEN_tok_gemini_prod_demo=AIza-REPLACE_ME

# ── Logging ───────────────────────────────────────────────────────────────────
RUST_LOG=info
# Use RUST_LOG=debug for verbose output including request/response details
